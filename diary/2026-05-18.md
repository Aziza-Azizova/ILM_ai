# 2026-05-18

## What was done

Kicked off the capstone project. Spent most of the time on decisions and setup rather than features — chose the stack (NestJS, PostgreSQL + pgvector, React + MUI, Claude + OpenAI embeddings), wrote the 4-week plan, designed the database schema, and scaffolded both backend and frontend. Got Docker Compose running with PostgreSQL and ran the initial migration that creates all 12 tables including the pgvector `document_chunks` table.

## Problems encountered

NestJS compiled frontend files by mistake because `tsconfig.build.json` had no scope restriction. Hit several TypeScript strict-mode errors across the backend on the first build.

## Solutions (if any)

Added `"include": ["src/**/*"]` to `tsconfig.build.json`. Fixed TypeScript errors by adding explicit types where needed.

## Next plan

Build JWT auth, Google OAuth, file upload pipeline (PDF/DOCX/TXT → chunk → embed → pgvector), and RAG chat with SSE streaming.

## Time spent

Lost track, probably too long :D

## Demo video

There will be one loom video for this week, please see 2026-05-29.md